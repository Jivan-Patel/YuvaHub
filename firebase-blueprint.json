{
  "entities": {
    "User": {
      "title": "User Profile",
      "description": "Stores user preferences and profile information.",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "Firebase Auth UID" },
        "email": { "type": "string", "format": "email" },
        "location": { "type": "string" },
        "age": { "type": "number" },
        "interests": { "type": "array", "items": { "type": "string" } },
        "notificationsEnabled": { "type": "boolean" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "email", "location", "age", "interests"]
    },
    "NewsletterSubscriber": {
      "title": "Newsletter Subscriber",
      "description": "Stores email addresses for newsletter subscriptions.",
      "type": "object",
      "properties": {
        "email": { "type": "string", "format": "email" },
        "subscribedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["email", "subscribedAt"]
    },
    "Registration": {
      "title": "Event Registration",
      "description": "Records of users registering for specific events or schemes.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "userEmail": { "type": "string", "format": "email" },
        "userLocation": { "type": "string" },
        "userAge": { "type": "number" },
        "eventId": { "type": "string" },
        "eventTitle": { "type": "string" },
        "registeredAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "userEmail", "userLocation", "userAge", "eventId", "eventTitle", "registeredAt"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "User profile document indexed by Auth UID."
    },
    "/newsletter/{email}": {
      "schema": "NewsletterSubscriber",
      "description": "Newsletter subscription document indexed by email."
    },
    "/registrations/{regId}": {
      "schema": "Registration",
      "description": "Collection of all user registrations for events."
    }
  }
}
